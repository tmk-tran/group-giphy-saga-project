// import Card from "@mui/material/Card";
// import CardContent from "@mui/material/CardContent";
// import Typography from "@mui/material/Typography";
// import { useSelector, useDispatch } from 'react-redux';
// import FavoritesListItem from "../FavoritesListItem/FavoritesListItem";
// import { useEffect } from "react";

export default function FavoritesList() {
  // const dispatch = useDispatch();
  //   const favorites = useSelector((state) => state.favorites.favoritesReducer);
// console.log('logging favorites in favorites list: ', favorites);
  //   useEffect(() => {
  //     dispatch({ type: 'FETCH_FAVORITES' }); 
  // }, [dispatch]);

  return (
    <div>
      <h1>Favorites Page</h1>
      {/* <div>
            {favorites.map((favorite) => (
                <li key={favorite.id}>
                <FavoritesListItem  favorite={favorite} />
                </li>
            ))}
        </div> */}
    </div>
  );
}



{
  /* <TableContainer>
      <Table>
        <TableHead>
          <TableRow>
            <TableCell>Change Me</TableCell>
          </TableRow>
        </TableHead>
        <TableBody>
            <TableRow>
                <TableCell>Table Cell-ChangeME</TableCell>
            </TableRow>
        </TableBody>
      </Table>
    </TableContainer> */
}
